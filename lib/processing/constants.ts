export const MAX_RAW_PROCESSING_ATTEMPTS = 5;

export const LEAD_EVENT_TYPE_CREATED = "created" as const;

/** Clock skew window for HMAC timestamp validation (seconds). */
export const HMAC_MAX_SKEW_SECONDS = 300;
